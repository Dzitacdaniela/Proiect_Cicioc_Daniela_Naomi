@page
@model Proiect_Cicioc_Daniela_Naomi.Pages.LaboratorCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Laborator[0].Denumire_Laborator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Laborator[0].Adresa_Laborator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Laborator) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denumire_Laborator)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adresa_Laborator)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
