@page
@model Proiect_Cicioc_Daniela_Naomi.Pages.ClientCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].Nume_Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].EMail_Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].Analize)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].Laboratoare.Denumire_Laborator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].Laboratoare.Adresa_Laborator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Client) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume_Client)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EMail_Client)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Analize.Denumire_Analiza)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Laboratoare.Denumire_Laborator)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Laboratoare.Adresa_Laborator)
               </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
